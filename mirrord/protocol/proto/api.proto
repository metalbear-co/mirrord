syntax = "proto3";

package mirrord.api;

message Empty {}

message BincodeMessage {
  bytes encoded = 1;
}

service Agent {
  rpc LayerConnect (stream BincodeMessage) returns (stream BincodeMessage) {}
}

