Added a new field for the `MirrordClusterSession` CRD, `SessionCiInfo`. Now the mirrord license-server can keep track of sessions that have been started using the `mirrord ci start` command, and we can validate concurrent CI seats.
