Bumped the shared `@metalbear/ui` component kit to 0.3.1 across the `mirrord ui` frontend packages (`packages/ui`, `packages/monitor`, `packages/wizard`) to pick up its stricter lint rules, and fixed every violation they surfaced with no suppressions (hardcoded strings moved into the per-feature strings modules, plus keys, guards, and memoization fixes).
