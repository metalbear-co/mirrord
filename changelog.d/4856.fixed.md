Fixed `mirrord container` failing on startup with `unexpected EOF when reading stdout` since 3.252.0: the intproxy sidecar panicked building an HTTP client for the local daemon because the CLI container image has no system CA certificates. The client is now built fallibly and the `mirrord-cli` image installs `ca-certificates`.
