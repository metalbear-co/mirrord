Added the `operator_isolation_marker` config option. When set, mirrord labels the database branch and preview session CRDs it creates with `operator.metalbear.co/owner`, so an isolated operator (run with a matching `OPERATOR_ISOLATION_MARKER`) reconciles them instead of the deployed operator. These CRDs are created directly against the Kubernetes API, so the `baggage` HTTP filter could not route them before.
