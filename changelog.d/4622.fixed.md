Fixed the layer's internal proxy connection breaking when the process is started with a closed standard fd. The connection socket could be assigned fd 0-2 and get reconfigured by the application's runtime (e.g. `libuv` setting `O_NONBLOCK` on stdin), killing the process with "Resource temporarily unavailable". This broke Next.js with Turbopack, which spawns its worker processes with stdin closed.
