Introduce sessions-manager-backed outgoing support: add the client/protocol plumbing for control-plane registration, room resolution, and multiplexed data-plane handoff between the agent and intproxy, and add a Linux serverless bootstrap build flow that packages the agent binary together with the bootstrap shared library.

Add the `capabilities-rust` sample and refresh its Next.js frontend to support switching between mirrored and plain backends, persisting backend endpoints locally, and rendering responses in a more structured way.
