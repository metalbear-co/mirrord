Pin every step of the release workflow to the release branch tip, so the
published binaries, images, GitHub release, package manifests and docs all
describe exactly the set of changes in this changelog entry, even when other
work lands on `main` while the release is in flight.
