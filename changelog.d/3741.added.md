Added support for node.js' `fs.copyFile` function. Note that the source/destinations files must be on the same machine, that is, the operation must be local → local or remote → remote. Cross-machine copying may be supported in the future.
