Fixed Next.js dev server crashing under mirrord when Turbopack compiles CSS. Turbopack's pooled Node workers talk to the parent over a loopback TCP channel; mirrord now detects Next.js processes and keeps that loopback IPC local (via outgoing `ignore_localhost`) so the worker handshake succeeds.
