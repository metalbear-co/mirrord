The `mirrord ui` session monitor and the `mirrord wizard` config wizard are now a single web app
served by `mirrord ui`. `mirrord wizard` opens the same app on the config wizard tab, and both
features share one server, one theme, and a light/dark toggle.
