Add a Settings dialog to the session monitor UI (accessed via the gear icon in the header) with a toggle for anonymous usage analytics. The preference persists in the browser's local storage. When turned off, the client calls `posthog.opt_out_capturing` and stops any active session recording immediately; the session's own `config.telemetry = false` still vetoes telemetry regardless of the UI toggle.
