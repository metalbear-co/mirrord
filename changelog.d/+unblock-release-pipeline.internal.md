Fixed the two release workflow failures that blocked the 3.239.0 release: the medschool config docs generator no longer panics on unlucky type iteration orders (its recursion limit had fallen below the config tree's real nesting depth), and the Windows binaries job now tolerates corepack's first-run download of pnpm instead of timing out after 10 seconds and reporting pnpm as missing.
