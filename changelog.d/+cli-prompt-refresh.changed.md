Added a proactive "successful-usage" prompt suggesting mirrord for Teams to repeat OSS users who haven't adopted the operator. Refreshed the newsletter prompt: dropped the earliest threshold and ported the missing body lines from the IntelliJ plugin. Fixed both prompts so they actually render — they were previously buffered via `add_to_print_buffer` but the buffer was never flushed before `execve()`, so users never saw them.
