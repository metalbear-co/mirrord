Fixed the Windows layer aborting a target process with "current thread handle already set during thread spawn" when it reported a logging problem. Three `eprintln!` calls ran on the `DllMain` path, and `eprintln!` takes a reentrant lock that claims the thread's Rust handle slot, which makes the next Rust thread the layer spawns abort the whole process. They now write to the standard error handle directly.
