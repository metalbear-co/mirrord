Enabled `TCP_NODELAY` on every socket used to relay mirrord traffic - layer to internal proxy, internal proxy to external proxy and to the agent, agent outgoing and passthrough connections, redirected incoming connections, and the local sockets used for intercepted and port-forwarded traffic. Relayed data was already framed by the application on the other end of the hop, so Nagle's algorithm only added latency to small writes.
