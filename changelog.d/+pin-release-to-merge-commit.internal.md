Pin every step of the release workflow to the release PR's merge commit, so the
published binaries, images, GitHub release, package manifests and docs all
describe the same commit even when other changes land on `main` while the
release PR is open.
