Fixed passthrough requests failing when the target application listens on the pod's external IP instead of loopback. This is fixed by the new `agent.external_ip_fix` option, enabled by default in the open source CLI.
