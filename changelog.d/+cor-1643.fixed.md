Fixed passthrough requests failing when the target app listens on the pod's external IP instead of loopback. The new `agent.external_ip_fix` option, enabled by default in the open source CLI, fixes this.
