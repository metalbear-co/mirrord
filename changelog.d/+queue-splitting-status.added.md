Added `mirrord queues` (alias `mirrord qs`) to browse the status of active queue-splitting sessions, including each session's phase, whether it is ready, its queues, and which target pods are patched and ready.
