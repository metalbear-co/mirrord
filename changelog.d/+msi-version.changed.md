Due to the Windows MSI installer limit of 255 for the major and minor version components, 
updated mirrord's MSI installer versioning scheme so that a CLI version of `3.256.1` is 
mapped to an MSI installer version of `1.3.2561`.

