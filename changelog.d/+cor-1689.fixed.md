`mirrord operator status` now labels CI and preview environment session counts as nonhuman concurrent sessions, and no longer prints a dangling section header when the operator does not report those counts.
