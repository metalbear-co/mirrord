The `mirrord ui` session monitor now captures richer HTTP detail on the event stream: request headers, a paired response event with status and headers, and request/response body previews, each carrying a per-exchange correlation id so a request and its response can be matched. Body capture is bounded by the new `experimental.session_monitor_body_limit` option (default 32KB; `0` disables body capture, keeping headers and metadata).
