Fix newsletter sign-up URLs concatenating the prompt count and the `cmd` suffix directly into `utm_source`, producing mangled values like `utm_source=newsletter5`, `newsletter20`, `newsletter100`, `newslettercmd`. The session count and command-vs-prompt distinction now travel as `utm_content` (e.g. `utm_content=prompt-5`, `utm_content=command`), so analytics consistently see `utm_source=newsletter`.
