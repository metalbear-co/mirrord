Renewing an expired client certificate no longer requests a CI credential from the operator.
Users whose stored certificate had expired failed to start a session with `Enterprise license is
required for generating mirrord CI api key` unless the operator ran on an Enterprise license.
