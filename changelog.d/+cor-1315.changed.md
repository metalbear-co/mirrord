Added new agent config `single_pod_multi_container: bool` that allows the agent to create dynamically named chain names for iptables rules, which allows running multiple mirrord sessions targeting the same pod, but for different containers.
